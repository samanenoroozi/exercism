def is_isogram(string):
    if string != "":
        string = string.lower().replace("-", "").replace(" ", "")
        count = 0
        for char in string:
            if string.count(char) > 1:
                return False
    return True
